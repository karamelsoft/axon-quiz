version: "3.7"

services:
  customers-command:
    image: axonquiz/customers-command:latest
    environment:
      SPRING_PROFILES_ACTIVE: swarm
    networks:
      - axon-quiz

networks:
  axon-quiz:
    external: true
