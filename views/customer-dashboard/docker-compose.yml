version: "3.7"

services:
  customer-dashboard-query:
    image: axonquiz/customer-dashboard-query:latest
    environment:
      SPRING_PROFILES_ACTIVE: swarm
    networks:
      - axon-quiz

networks:
  axon-quiz:
    external: true
